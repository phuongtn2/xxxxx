import { FromCharCodePipe } from './from-char-code.pipe';

describe('FromCharCodePipe', () => {
  it('create an instance', () => {
    const pipe = new FromCharCodePipe();
    expect(pipe).toBeTruthy();
  });
});
